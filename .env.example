# Copy this file to .env.local and fill in your own key.
# Get one at https://platform.openai.com/api-keys
# Used server-side only by app/api/chat/route.js — never exposed to the browser.
OPENAI_API_KEY=
